services:
  sbs:
    image:  yuwenhaibo/sbs:latest
    environment:
      - SNI=www.samsung.com
    container_name: sbs
    restart: always
    ports:
      - "443:443"
    volumes:
      - sbs-volume:/opt/singbox/config
volumes:
  sbs-volume: